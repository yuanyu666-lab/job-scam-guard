# 手机防骗 · 零基础一条龙（两步都卡了就看这个）

不用买云服务器，不用手机和电脑同一 WiFi。  
用 **Cloudflare 免费隧道** 把家里电脑上的 API 临时变成 `https://xxx.trycloudflare.com`，手机用流量也能连。

---

## 总流程（只记 2 件事）

| 顺序 | 做什么 | 耗时 |
|------|--------|------|
| A | 电脑：双击脚本 → 得到 HTTPS 地址 | 约 5 分钟 |
| B | 手机：装 APK → 填地址 → 开悬浮球 | 约 10 分钟 |

APK 两种拿法任选其一：**GitHub 自动打包下载**（不用 Android Studio）或 **自己装 Android Studio**（见文末）。

---

## A. 云端 API（其实是你电脑 + 隧道）

### 第一次准备（只做一次）

1. 电脑已安装 **Python 3.10+**（开始菜单能搜到 Python）  
2. 双击项目里的 **`首次安装依赖.bat`**，等它跑完（可能要 10～20 分钟）  
3. 安装隧道工具（二选一，推荐 winget）：

```powershell
winget install Cloudflare.cloudflared
```

装完**关掉并重新打开** PowerShell / 命令提示符。

### 每次要用手机检测时

1. **关掉**电脑上的 `python run.py` / 悬浮窗（避免占端口）  
2. 双击 **`scripts\手机公网隧道.bat`**  
3. 会弹出两个黑窗口，不要关：
   - 一个叫「招聘防骗-API」
   - 一个叫「招聘防骗-隧道」  
4. 看「隧道」窗口里会出现一行类似：

   ```
   https://abcd-1234.trycloudflare.com
   ```

5. 项目根目录会生成 **`手机填这个地址.txt`**，把里面地址复制到手机 App  
6. **电脑要一直开着**，隧道窗口不能关；手机才能随时检测  

### 检查是否成功

手机浏览器（用流量 4G/5G，别开和电脑同一个 WiFi 也行）打开：

`https://你的地址/api/mobile/status`

能看到 `"ok": true` 就对了。

### 常见问题 A

| 问题 | 解决 |
|------|------|
| 提示找不到 cloudflared | 执行 `winget install Cloudflare.cloudflared` 后重启电脑再试 |
| 隧道窗口一闪就关 | 右键脚本 → 用管理员运行；或先手动运行 `首次安装依赖.bat` |
| 手机打不开网址 | 隧道窗口是否还在；API 窗口有没有报错 |
| 很慢 | 正常，截图要上传到你电脑 OCR |

---

## B. 安装 App（夸克式小悬浮球）

### 办法 1：GitHub 自动打包（推荐，不用 Android Studio）

1. 把项目推到你的 **GitHub 仓库**（私有也行）  
2. 打开 GitHub 网页 → 你的仓库 → **Actions**  
3. 左侧点 **Build Android APK** → **Run workflow** → 运行  
4. 等约 5～10 分钟变绿 → 点进这次运行 → **Artifacts** → 下载 `app-debug-apk`  
5. 解压得到 `app-debug.apk`，传到 Vivo 手机安装  

（仓库里已配好工作流：`.github/workflows/build-android-apk.yml`）

### 办法 2：自己用 Android Studio（本地）

1. 安装 Android Studio：https://developer.android.com/studio  
2. 打开文件夹 **`mobile\android`**（不是整个 job-scam-guard）  
3. 等右下角 Gradle 完成（第一次很久）  
4. USB 连手机，开 USB 调试 → 点绿色 ▶ Run  

更细步骤：`mobile\android\制作APP教程.md`

### 手机里必做设置（Vivo X300 Pro）

1. 打开 **招聘防骗**  
2. **服务器地址** 粘贴 `手机填这个地址.txt` 里的 `https://...`（不要末尾 `/`）  
3. 点 **授权悬浮窗** → 系统里允许  
4. 点 **授权屏幕录制** → 点「立即开始」  
5. 点 **启动悬浮球**  
6. **设置 → 电池 → 招聘防骗 → 不限制后台**  

### 使用

打开 BOSS / 微信 → 点边上蓝色 **盾** → 框选文字 → 等结果卡片。

---

## 你还是卡在哪？

对着做，卡在某一步时看下面：

### 卡：首次安装依赖失败

- 换网络重试  
- 或只装手机必需：`pip install fastapi uvicorn python-multipart httpx pillow rapidocr-onnxruntime mss numpy`

### 卡：没有 GitHub

- 只能用 **办法 2 Android Studio**，或请朋友帮你跑一次 GitHub Actions 把 apk 发你  

### 卡：Vivo 不让装 apk

- 设置里允许「安装未知应用」  
- 或用 Android Studio USB 直接 Run 到手机（不用 apk 文件）  

### 卡：悬浮球不显示

- 必须先在 App 里点「授权悬浮窗」  
- OriginOS：设置搜索「悬浮窗」→ 招聘防骗 → 允许  

---

## 文件索引

| 文件 | 作用 |
|------|------|
| `scripts\手机公网隧道.bat` | 一键 API + 公网 HTTPS |
| `手机填这个地址.txt` | 脚本生成的地址（运行后有） |
| `mobile\android\制作APP教程.md` | Android Studio 详细版 |
| `.github/workflows/build-android-apk.yml` | 云端自动打 APK |
